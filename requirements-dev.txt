# ─────────────────────────────────────────────────────────────────
# Dev / test dependencies (not installed in Lambda image)
# Install: pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────

# ── Testing ───────────────────────────────────────────────────────
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<0.25.0
pytest-cov>=6.0.0,<7.0.0
moto[dynamodb,s3,secretsmanager]>=5.0.0,<6.0.0   # AWS service mocking

# ── Linting / formatting ──────────────────────────────────────────
ruff>=0.7.0,<0.8.0
mypy>=1.13.0,<2.0.0
boto3-stubs[dynamodb,s3,secretsmanager,bedrock-runtime]>=1.35.0

# ── Local dev utilities ───────────────────────────────────────────
ipython>=8.28.0
rich>=13.9.0                         # pretty local output
